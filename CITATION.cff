# Citation metadata for nmrForge.
#
# STATUS: usable as a citation for the repository itself. The author is confirmed; the affiliation,
# the repository URL and a DOI are still to be added after the GitHub repository and the Zenodo
# record exist (see RELEASE_CHECKLIST_v0.9.0.md). Until a DOI exists, cite the repository URL.
#
# When the repository is public, add:
#   repository-code: "https://github.com/<owner>/nmrforge"
# and after a Zenodo release:
#   doi: "10.5281/zenodo.XXXXXXX"
#   preferred-citation: { ... }   # only if a software paper exists
#
# The licence below must not be changed without updating LICENSE, pyproject.toml, README.md and
# LICENSE_OPTIONS.md together (a test enforces that they agree).

cff-version: 1.2.0
message: "If you use nmrForge, please cite this metadata (repository URL until a DOI exists)."
title: "nmrForge: automation, parameter optimisation and quality control for Bruker multidimensional NMR data"
abstract: >-
  nmrForge is a platform for automating Bruker multidimensional NMR data processing. It reads
  Bruker acquisition parameters, identifies the experiment type and the sampling scheme, plans a
  processing strategy, drives NMRPipe (and SMILE for non-uniform sampling reconstruction) to
  execute it, performs quality control on the result, and records the resolved parameters,
  external tool versions and warnings so that a run can be reproduced and audited.
type: software
authors:
  - family-names: "Li"
    given-names: "Xuanfeng"
    # affiliation: add when confirmed (institute / laboratory)
version: 0.9.0
license: "Apache-2.0"
keywords:
  - NMR
  - NMRPipe
  - Bruker
  - non-uniform sampling
  - NUS
  - SMILE
  - spectrum processing
  - quality control
  - reproducibility
  - scientific software
