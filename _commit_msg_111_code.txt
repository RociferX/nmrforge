refactor: 实验模板单一数据源 presets/*.yaml,删除 Python 重复模板;合并 ext/线宽默认值(0.2.111)

- core/experiments/registry:实现 ExperimentTemplate.from_yaml 与
  load_presets(按显示名 + 文件 stem 双注册,Generic/大小写均可解析);
  __init__ 改为导入即加载 presets/*.yaml;删除 15 个逐模块 Python 模板
  (cbcaconh/cbcanh/cosy/generic/hmbc/hmqc/hnca/hncacb/hnco/hnco_ca/
  hnha/hsqc/noesy/roesy/tocsy),presets 成为唯一数据源;
- experiment_classifier 显式 import core.experiments,保证 REGISTRY 已加载;
- 顺带合并重复默认值:GUI param_schema 兜底骨架与对话框 ext_lo/ext_hi
  回退改读 backend.config.load_processing_defaults(消除硬编码);清理
  config.py 过时注释(script_generator 已无 _DEFAULT_LINEWIDTH_HZ);
- presets/hncacb.yaml priors 补 13C_alpha/13C_beta(保留 Python 版细分);
- 测试:冒烟新增 YAML 全注册/stem 别名/分类器 pulprog 名单防漂移;
  test_gui_processing 新增 GUI 默认值与后端一致断言;本地全量 535 passed,
  ruff 全绿。
